apiVersion: v1
kind: Service
metadata:
  name: nginx-tcp
spec:
  type: LoadBalancer
  selector:
    app: nginx-tcp
  ports:
{{- range .Values.clients }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
{{- end }}
