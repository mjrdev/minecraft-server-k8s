apiVersion: v1
kind: Service
metadata:
  name: nginx-tcp
spec:
  type: NodePort
  selector:
    app: nginx-tcp
  ports:
{{- range .Values.clients }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
{{- end }}
